---
import Layout from "../layouts/Layout.astro";
import { homePageContent, identity } from "../data";
import SocialLink from "../components/SocialLink.astro";
---

<Layout seo={homePageContent.seo}>
  <section class="mt-10 max-w-2xl mx-auto px-6">
    <h1 class="text-3xl mb-1">{identity.name}</h1>
    <p class="opacity-60 mb-6 subtitle">{homePageContent.role}</p>
    <!--NOTE: keep as example-->
    <!--<div class="flex flex-row flex-wrap gap-6 mb-8">-->
    <!--<Image-->
    <!--  src={identity.logo}-->
    <!--  alt={identity.name}-->
    <!--  width={100}-->
    <!--  height={100}-->
    <!--  loading="lazy"-->
    <!--  decoding="async"-->
    <!--  quality={100}-->
    <!--  densities={[1.5, 2]}-->
    <!--  class="rounded-full"-->
    <!--/>-->
    <div class="group flex flex-row justify-center gap-2 mb-8 w-fit">
      Let's connect:
      {
        homePageContent.homeSocialLinks.map((link) => (
          <SocialLink title={link.title} url={link.url} icon={link.icon} />
        ))
      }
    </div>
    <!--</div>-->
    <p class="max-w-md mb-8">
      Tinkerer specializing in building scalable, secure, and
      performant applications using <strong>TypeScript</strong>, <strong>Java</strong>, and <strong>Python</strong>.
      <br /><br />
    </p>

    <p class="max-w-md mb-2">Here is my tech toolbox:</p>
    <!--TODO: update UI-->
    Node.js (Nest.js), Spring, Django, PostgeSQL, AWS, Docker
  </section>
</Layout>
