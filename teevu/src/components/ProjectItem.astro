---
import type { ProjectItem } from "../types/index.type";

type Props = ProjectItem;

const { title, description, date, url } = Astro.props;
---

<a
  href={url}
  class="py-3 flex items-center justify-between group-hover:opacity-60 hover:!opacity-100 transition-opacity"
>
  <div class="flex justify-between items-center gap-4">
    <!--<Image-->
    <!--  src={company.image}-->
    <!--  alt={company.name}-->
    <!--  width={40}-->
    <!--  height={40}-->
    <!--  loading="lazy"-->
    <!--  decoding="async"-->
    <!--  quality={100}-->
    <!--  densities={[1.5, 2]}-->
    <!--  class="rounded-full w-[40px] h-[40px] object-cover"-->
    <!--/>-->
    <div>
      <h3 class="font-medium">{title}</h3>
      <p class="text-sm opacity-60">{description}</p>
    </div>
    <p class="text-sm opacity-60">{date}</p>
  </div>
</a>
